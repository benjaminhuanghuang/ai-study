Sign into your Azure account (Ctrl + Shift + p),
```
    >Azure: Sign In
```
Choose your default workspace by leveraging the command palette (Ctrl + Shift + p) and type 
```
    Azure ML: Set Default Workspace. 
```
